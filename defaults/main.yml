---
# defaults file for coreos-StaticAddr

# --- Preset Sample ---
# You need to set inet interface, network and gateway addresses 
# 
# - { inet: eth0, network: 10.0.0.0/8, gateway: 172.17.12.1 }
# ...
#

addrs:
  - { inet: eth0, network: 192.168.152.0/30, gateway: 10.12.13.15 }
